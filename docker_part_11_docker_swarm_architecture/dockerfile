# Use the official Ubuntu base image
FROM ubuntu:latest

# Set a working directory
WORKDIR /app

# Run a simple command to keep the container alive (optional, for testing)
CMD ["tail", "-f", "/dev/null"]
